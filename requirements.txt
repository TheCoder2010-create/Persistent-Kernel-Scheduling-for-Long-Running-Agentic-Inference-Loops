# Requirements for D:\research
# Python 3.13.13 | CUDA Toolkit 13.3 | Driver 610.47
# GPU: NVIDIA GeForce RTX 4050 Laptop GPU (sm_89, Ada Lovelace)
#
# Install torch with CUDA 12.8 wheels:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# Install remaining packages:
#   pip install -r requirements.txt

# Core
torch==2.11.0+cu128
torchvision==0.26.0+cu128
torchaudio==2.11.0+cu128

# Triton: ships bundled with PyTorch on Windows — no separate install needed
# (standalone 'triton' PyPI package has no Windows wheel)

# Numerics & plotting
numpy==2.4.4
matplotlib==3.10.3

# Deps (pinned transitively)
filelock==3.29.7
fsspec==2026.6.0
jinja2==3.1.6
markupsafe==3.0.3
mpmath==1.3.0
networkx==3.6.1
sympy==1.14.0
typing_extensions==4.16.0
pillow==12.2.0
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
packaging==26.2
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
